# Requirements for clone_repo.py
# This script only uses standard library modules, so no external dependencies are required.
# Python 3.6+ is recommended.

# If you need additional functionality in the future, you can add dependencies here:
# requests>=2.25.0  # For HTTP-based operations
# gitpython>=3.1.0  # For advanced Git operations